require "spec_helper"

describe DatabaseStalker do
  it "has a version number" do
    expect(DatabaseStalker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
